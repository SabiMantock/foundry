# Foundry — environment template. Copy to .env (gitignored). Secrets are injected
# from the secret store in CI/prod; never commit real values (standards doc 06 §2).
NODE_ENV=development
DATABASE_URL=postgres://localhost:5432/foundry
REDIS_URL=redis://localhost:6379
LOG_LEVEL=info
